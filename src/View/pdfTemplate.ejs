<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product PDF Template</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <style>
        .card-title{
               color:brown;
        }
        .product-card {
            max-width: 400px;
            margin-bottom: 20px;
            margin-top: 35px;
            background-color: rgba(230, 255, 248, 0.637);
        }
    </style>
</head>
<body>
    <div class="container d-flex justify-content-center margin-5">
        <% data.forEach(product=> { %>
            <div class="card product-card">
                <div class="card-body">
                    <h5 class="card-title"><strong>Name:</strong><strong><%= product.name %></strong>
                    </h5>
                    <p class="card-text"><strong>Created Date:</strong>
                        <%= new Date(product.created_date).toLocaleDateString('en-US') %>
                    </p>
                    <p class="card-text"><strong>Type:</strong>
                        <%= product.type %>
                    </p>
                    <p class="card-text"><strong>Price:</strong> $<%= product.price %>
                    </p>
                    <p class="card-text"><strong>Description:</strong>
                        <%= product.description %>
                    </p>
                    <div class="row ">
                        <div class="product-img margin-5 d-flex">
                            <% product.images.forEach(image=> { %>
                                <div class="col">
                                    <img src="<%= image.image %>" class="card-img-top" alt="Product Image">
                                </div>
                                <% }) %>
                        </div>

                    </div>
                </div>
            </div>
            <% }) %>
    </div>
</body>

</html>